import numpy as np

def squared_array(arr):
    """
    Squares each element in a NumPy array.

    Parameters:
        arr (numpy.ndarray): Input array.

    Returns:
        numpy.ndarray: Array with each element squared.
    """
    return np.square(arr)
